public class operators {
    public static void main(String []args){
        int a =5;
        int b =6;
        boolean res1 = a<b; //lessthan operator   we also use <=
        boolean res2 = a>b; //greater than operator we also use >=
        boolean res3 = a==b; //equal to operator  
        System.out.println(res1);
        System.out.println(res2);
        System.out.println(res3);
        //conditional operaters used for comparision 
        // & and operator 
        // | or operator 
        // ! not operator 
        // && when both statements are true 
        // || in both statements only one statement is true 
        // ! comparing both   
        //boolean s = !(10 == 10) boolean s = !(true) boolean s = fales
    }
}
